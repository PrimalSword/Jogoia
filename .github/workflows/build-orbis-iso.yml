name: Build OrbisOS ISO

on:
  workflow_dispatch:
  push:
    branches:
      - orbis-os
    paths:
      - 'orbis-build.sh'
      - '.github/workflows/build-orbis-iso.yml'
      - 'ORBISOS.md'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y live-build debootstrap xorriso isolinux syslinux-common

      - name: Build OrbisOS
        run: |
          chmod +x orbis-build.sh
          sudo ./orbis-build.sh

      - name: Show generated files
        run: ls -lh dist/

      - name: Upload ISO
        uses: actions/upload-artifact@v4
        with:
          name: OrbisOS-0.1-amd64
          path: |
            dist/*.iso
            dist/*.sha256
          if-no-files-found: error
          retention-days: 14
